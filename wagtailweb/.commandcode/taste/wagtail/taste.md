# wagtail
- Button background color should reference the brand color field rather than using a hardcoded default hex. Confidence: 0.60
- Button style config (border, padding, colors) needs to be implemented across all section layout templates, not just base.html and CSS. Confidence: 0.60
- Remove the button_shape field — button shape should not be a separate configurable option. Confidence: 0.65
- When creating a new home page, populate sections with dummy/filler content so the user can visualize the template layout. Confidence: 0.80
- For vertical-axis properties like section height, use names that describe vertical dimension (e.g., compact, tall) rather than horizontal terms (e.g., narrow, wide). Confidence: 0.72
- For pixel-based height fields in Wagtail settings, use an IntegerField (number input) rather than a CharField, so users type a numeric pixel value directly. Confidence: 0.75
- Manage snippet data (like color themes) inline on the settings/edit page rather than via a separate button linking to the snippet list. Confidence: 0.75
- Always pick the first ColorTheme as the default global theme (always set a default, don't leave it nullable/unset). Confidence: 0.78
- Section-specific overlay/shade color and opacity fields belong in each section's content panel (not in global color theme settings or section settings tab). Confidence: 0.70
- Design global color settings around concrete component-level CSS properties (button bg, button hover, heading colors, paragraph text, link colors, page background) rather than abstract semantic tokens (brand, primary, secondary). Confidence: 0.78
- Avoid creating an abstract combined base class from ClusterableModel + BaseSiteSetting — ClusterableModel is a concrete model, so multi-table inheritance causes FK constraint failures on get_or_create. Use a different approach (e.g., keep settings as BaseSiteSetting with models.ForeignKey on link models, but note that InlinePanel requires ParentalKey which requires ClusterableModel). Confidence: 0.85
- For reusable content like navigation links and social media links, prefer creating Wagtail snippets (with per-group labels and tree ordering) that can be referenced by the footer, rather than managing links manually inline in footer settings. Confidence: 0.70
- Override `_navigation_explore.html` (in `templates/wagtailadmin/pages/listing/`) to change the navigation column icon and URL for each page row — for example, replacing the arrow-right → explore with a pencil icon → edit. Confidence: 0.65
- Override `_page_title_explore.html` (in `templates/wagtailadmin/pages/listing/`) to change where the page title link in the page listing points (e.g., from edit URL to explore URL) using `wagtailadmin_explore` instead of `wagtailadmin_pages:edit`. Confidence: 0.65
- Register a `register_page_listing_more_buttons` hook to add custom buttons (e.g., "Explore" with arrow icon linking to explore URL) to the page listing actions dropdown, by creating a subclass of `PageListingEditButton` that overrides `label`, `icon_name`, and `url_name`. Confidence: 0.60
- `register_page_listing_more_buttons` is additive — you can't remove the default "Edit" button from Wagtail's built-in hook via hooks alone. To replace it, either: (a) use `construct_page_listing_buttons` (operates on outer wrapper buttons, not inner dropdown items), (b) override `_button_with_dropdown.html` template to filter by label, or (c) register your own hook that yields a replacement with the same priority and also check `page.is_navigable` in the dropdown template. Confidence: 0.70
- Override `_button_with_dropdown.html` template to filter out specific dropdown items (e.g., hide the default "Edit" button) by checking `button.label` and `button.icon_name` in the for loop. Confidence: 0.65
